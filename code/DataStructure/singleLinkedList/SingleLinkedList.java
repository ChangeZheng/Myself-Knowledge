package DataStructure.singleLinkedList;
/**
 * 此类为有头结点的单向链表，用于管理节点HeroNode，
 * @author ZXY
 *
 */
public class SingleLinkedList {
	//头结点：数据域为空，指针域指向第一个节点(初始化也为空)
	public HeroNode head = new HeroNode();
	
	
}